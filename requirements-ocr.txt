# Распознавание сканов. Ставится поверх requirements.txt и только там,
# где нужно обрабатывать PDF без текстового слоя — тянет ~3 ГБ моделей.
# Без этого пакета приложение работает, но на скан отвечает понятной ошибкой.
-r requirements.txt
mineru[core]==3.4.5
